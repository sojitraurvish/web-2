//  if you have js then only install jest
// if you have ts then install ts-jest and @jest/globals
// npm install --save-dev ts-jest  @jest/globals

// initialize jest.config.js file 
// npx ts-jest config:init

// wehn run test then jest goes to you entire folder and test all the files
// .test.ts or .spec.ts files will be tested