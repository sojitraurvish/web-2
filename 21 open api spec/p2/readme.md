when you change field in your backend then other people who is using your api in there backend they also have change and that is why this
openapispec(clients), graphql and grpc

also if you give this opoenapispec.json file to gpt it can hit your endpoints 

here we auto generate openapispec file using some libraries


// check classess code - what is decorators
helpful-decorator - @once // also check this library for how you can create your own decorators
// to compile decoratores code wit ts compiler we need to use 
// tsc --emitDecoratorMetadata src/server.ts
@measure - to check how much time is taken by the funciton