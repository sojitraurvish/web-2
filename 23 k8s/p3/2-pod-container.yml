# see iamge 1 
apiVersion: v1
kind: Pod
metadata:
  name: nginx # this is name for it and it goes in metadata
spec:
  containers: # it can runing multiple container but we are just giving only one container right now, if i add multiple then single pod start multiple containers
  - name: nginx-container-1
    image: nginx
    ports:
    - containerPort: 80
  - name: nginx-container-2
    image: nginx
    ports:
    - containerPort: 80


apiVersion: v1
kind: Pod
metadata:
  name: nginx-pod-2 # this is name for it and it goes in metadata
spec:
  containers: # it can runing multiple container but we are just giving only one container right now, if i add multiple then single pod start multiple containers
  - name: nginx-container-1
    image: nginx
    ports:
    - containerPort: 80